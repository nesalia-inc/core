# Documentation vs Implementation Gap Analysis

**Date:** 2026-05-11
**Project:** @deessejs/fp
**Scope:** packages/fp/src vs docs/deps/

---

## Summary

Cross-referencing the public API exports (`packages/fp/src/index.ts`) against the documentation files (`docs/deps/`) reveals **4 features documented but not implemented**, and **6 features implemented but not documented**. A key finding is that the Try pattern documentation (DEP-0004) references `Result.attempt()` which does not exist — the actual implementation uses `resultFromThrowable` and `resultFromNullable`. Additionally, the serialization docstrings contain incorrect examples showing static method syntax that does not exist.

Initial findings from the first analysis agent contained **3 false positives** — `Maybe.toResult()`, `Maybe.equals()/equalsWith()`, and `Maybe.race()` are all correctly exported from `maybe/index.ts`. These were corrected after verification.

---

## Part 1 — Features Documented but NOT Implemented (VERIFIED)

| Feature | Documentation | Verification Result |
|---------|--------------|---------------------|
| `Result.attempt()` / `Result.attemptAsync()` | DEP-0004 | ✅ **Confirmed** — does not exist. `resultFromThrowable` / `resultFromNullable` are the actual implementations |
| `Maybe.toResult()` | DEP-0003 | ✅ **Confirmed** — exists as method AND standalone function, exported from `maybe/index.ts` (line 18). Original report was incorrect. |
| `Maybe.equals()` / `Maybe.equalsWith()` | DEP-0003 | ✅ **Confirmed exported** — both are exported from `maybe/index.ts` (line 21). Original report was incorrect. |
| `Maybe.race()` | DEP-0003 | ✅ **Confirmed exported** — `race` is exported from `maybe/index.ts` (line 24). Original report was incorrect. |
| `Result.gen()` as static method | DEP-0001 | ✅ **Confirmed** — exists only as `gen()` standalone function in `result/builder.ts` (lines 437-471), not a static method on `Result` |
| `Error.reviver` vs `ErrorReviver` | DEP-0008 | ✅ **Confirmed naming discrepancy** — only `ErrorReviver` exists (`serialization/builder.ts:40`), not a type called `reviver` |
| Static method forms of serialize/deserialize | DEP-0008 | ✅ **Confirmed** — only standalone functions exist. Docstrings in `serialization/index.ts` lines 16-21 incorrectly show `Result.serialize(...)` and `Maybe.serialize(...)` as if they were static methods |

---

## Part 2 — Features Implemented but NOT Documented (VERIFIED)

| Feature | Location | Verification Result |
|---------|----------|---------------------|
| `resultFromNullable` | `conversions.ts` | ✅ **Confirmed** — exported from `index.ts` (line 429) but no mention in DEP-0004 |
| `resultFromThrowable` | `conversions.ts` | ✅ **Confirmed** — exported from `index.ts` (line 430) but no mention in DEP-0004 |
| `isAsyncResult()` | `index.ts` (line 209-211) | ✅ **Confirmed** — exported in unified API but not clearly documented |
| `sleepUntil` | `sleep.ts` | ✅ **Confirmed** — not mentioned in DEP-0007 |
| `addJitter` | `sleep.ts` | ✅ **Confirmed** — not documented |
| `immediate` | `yield.ts` | ✅ **Confirmed** — exported as alias for `yieldControl` but not documented |

**Note:** `toResult`, `equals`, `equalsWith`, and `race` on Maybe are all correctly exported — the original report incorrectly flagged them as missing.

---

## Part 3 — Naming Discrepancies (Docs vs Exports)

| Documentation Name | Actual Export Name |
|-------------------|-------------------|
| `Result.serialize(result)` | `serializeResult(result)` |
| `Result.deserialize(...)` | `deserializeResult(...)` |
| `Maybe.serialize(maybe)` | `serializeMaybe(maybe)` |
| `Maybe.deserialize(json)` | `deserializeMaybe(json)` |
| `Error.reviver` | `ErrorReviver` |
| `Result.attempt(fn)` | `resultFromThrowable(fn)` |
| `Maybe.race()` | Exported from `maybe/index.ts` (line 24) |
| `Result.fromPromise()` | `fromPromise()` in async-result module |

---

## Recommended Actions

1. **High priority:** Update DEP-0004 to document `resultFromThrowable` / `resultFromNullable` as the Try pattern replacements, and remove references to non-existent `Result.attempt()` / `Result.attemptAsync()`
2. **High priority:** Fix docstrings in `serialization/index.ts` (lines 16-21) — they incorrectly show `Result.serialize(...)` and `Maybe.serialize(...)` as static method calls
3. **Low priority:** Add `sleepUntil`, `addJitter`, and `immediate` to DEP-0007 and DEP-0001 respectively
4. **Low priority:** Add `isAsyncResult()` to DEP-0001 documentation
5. **Informational:** `ErrorReviver` naming — no action needed unless a rename is desired (document current name)

---

*Generated by Claude Code agent analysis*
